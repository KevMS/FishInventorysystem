// left and right arrow of slide image

function scrolll() {
    let left = document.querySelector('.about-scroll-images');
    left.scrollBy(-338, 1);
}

function scrollr() {
    let right = document.querySelector('.about-scroll-images');
    right.scrollBy(338, 1);
}